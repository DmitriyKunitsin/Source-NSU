s = poly(0, 's');

// параметры
k1 = 1;
k2 = 1;
k3 = 1;
T2 = 1;
T3 = 1;
d = 0.5;

// передаточная функция (как дробь)
G_poly = k1*k2*k3 / ((T2*s + 1)*((T3*s)^2 + 2*d*T3*s + 1));

// превращаем в систему
G = syslin('c', G_poly);

// Nyquist
nyquist(G);
